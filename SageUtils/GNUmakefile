### GNUmakefile automatically generated by WorkingWiki ###
WW_THIS_PROJECT_SOURCE_FILES = latex_output.py plot_vector_field_along_curve.py sage.mk python.mk step.mk step.pl GNUmakefile
PREREQUISITE_PROJECTS =
ifndef RESOURCES
  # ProjectEngine provides RESOURCES and the prerequisite project
  # locations.  If they aren't provided, we're in an exported directory.
  export RESOURCES = ../.workingwiki/resources
  export WW_PUBLISH_URL = /theobio/worden/index.php/Special:GetProjectFile?project=SageUtils&amp;make=false&amp;display=raw&amp;filename=
  export PUBLISH_URL = /theobio/worden/index.php/Special:GetProjectFile?project=SageUtils&amp;make=false&amp;display=raw&amp;filename=
  export WW_GPF_BASE_URL = /theobio/worden/index.php/Special:GetProjectFile?project=SageUtils&amp;filename=
  export WG_SITENAME = Worden
  export WG_SCRIPT_PATH = /theobio/worden
  export MW_PAGENAME = Special:ManageProject
endif
include $(RESOURCES)/makefile-before
ifeq ($(filter makefile, $(wildcard makefile)),makefile)
  include makefile
else ifeq ($(filter Makefile, $(wildcard Makefile)),Makefile)
  include Makefile
endif
include $(RESOURCES)/makefile-after
